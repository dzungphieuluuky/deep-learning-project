_target_: lightning.pytorch.Trainer

min_epochs: 1
max_epochs: 10
accelerator: "auto"    # Auto-detects GPU/MPS/CPU
devices: "auto"
precision: "16-mixed"  # Mixed precision training
log_every_n_steps: 10
check_val_every_n_epoch: 1
deterministic: False